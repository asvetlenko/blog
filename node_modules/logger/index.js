/**
 * Created by we on 19-Dec-15.
 */

//var logger = require('logger')(module);

module.exports = function(module){
    return {
        log:function(){
            var args = [].slice.call(arguments);
            args.unshift(module.filename);
            console.log.apply(console, args);
        }
    };
};
